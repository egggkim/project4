<!-- this needs to become a modal on the index view -->
<h1>Add a new event</h1>

<%= bootstrap_form_for @event do |f| %>
  <%= f.text_field :address %>
  <%= f.text_field :date %>
  <%= f.text_field :start_time %>
  <%= f.text_field :end_time %>

  <%= f.submit "Add this event!" %>
<% end %> 